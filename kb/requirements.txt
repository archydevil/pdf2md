# KB Forge — core sidecar deps (Fase 0/1)
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27
lancedb>=0.15
pyarrow>=17
numpy>=1.26
python-multipart>=0.0.12

# Parsing documenti (Fase 1) — installa quando passi alla Fase 1
# docling>=2.0
# python-docx>=1.1
# markdown-it-py>=3.0

# OCR (Fase 3) — fallback Ollama vision se assente
# rapidocr-onnxruntime>=1.3

# Privacy (Fase 4) — PII reversibile + air-gap egress (INSTALLATO)
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.8
# Modello spaCy IT: python -m spacy download it_core_news_sm

# STT (Fase 5) — backend faster-whisper offline (INSTALLATO)
# whisper.cpp è il backend primario quando presente (KB_WHISPER_BIN + ffmpeg + modello)
faster-whisper>=1.2

# MCP server (Fase 6) — espone la KB a qualsiasi client MCP (INSTALLATO)
mcp[cli]>=1.27